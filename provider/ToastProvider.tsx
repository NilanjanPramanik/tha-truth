'use client';

import { ToastContainer } from "react-toast";

const ToastProvider = () => {
  return (
    <div>
        <ToastContainer position="top-right"/>
    </div>
  )
}

export default ToastProvider